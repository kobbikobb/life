filters: 'file.hasLink("People")'

views:
  - type: table
    name: "Work"
    filters:
      and:
        - 'context == "work"'
    order:
      - file.name
      - org
      - role

  - type: table
    name: "All"
    order:
      - file.name
      - context
      - org
      - role
